import { verifyToken } from "../api/auth"

export const load = (async ({ cookies }) => {
  // let isVerified: boolean = false
  // let authToken = window.document.cookie.split(';').filter(s => s.includes('Authorization: '))
  // console.log(authToken)
  // if (authToken === undefined) {}
  // else {
  //   isVerified = await verifyToken(authToken.replaceAll('Bearer ', ''))
  // }

  // return {
  //   auth: authToken,
  //   isVerified: isVerified
  // }
})